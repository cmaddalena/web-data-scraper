{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "playwright install chromium --with-deps && python main.py --site zonaprop --search departamentos_venta_caba --max-pages 5",
    "restartPolicyType": "NEVER"
  }
}
